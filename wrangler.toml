name = "blitz-climb"
main = "worker.js"
compatibility_date = "2025-01-01"

# Fill in after: wrangler kv namespace create PROGRESS
[[kv_namespaces]]
binding = "PROGRESS"
id = "REPLACE_WITH_KV_NAMESPACE_ID"

# Set the admin token used by the weekly publisher (do NOT commit it):
#   wrangler secret put ADMIN_TOKEN
